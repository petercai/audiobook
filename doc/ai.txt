complete refactor operations in lib/functions.py step by step, the purpose is to break the large file to small pieces of code:
1. extract function web_interface() to new file web_ui.py
2. extract functions convert_ebook() and convert_ebook_batch() to new file headless_processor.py
3. all the shared dependencies still need to stay in functions.py
4. update all impacted python files when required



complete refactor operations in lib/functions.py step by step, the purpose is to break the large file to small pieces of code:
1. extract function convert_chapters2audio() to file epub.py
2. move sole deps to the same file, but the shared dependencies still need to stay in functions.py
3. update all impacted python files when required

move combine_audio_chapters() in functions.py to file ebook_audio.py with refactoring:
- move sole deps to the same file
- update all impacted python files when required

move all nested functions inside launch() to WebUI class method
- only update impacted reference code inside launch() for WebUI
- don't change any other existing logic
- don't try extra refactoring



refactor epub.py to a class EPubProcessor 
- convert2epub() is class method
- update all impacted python files when required

refactor tts text normalization function in class EPubProcessor in epub.py
- create new class TextNormalizer in file text_normalizer.py
- move method normalize_text() and all its dependencies to TextNormalizer class
- try best to move all text normalization function code to TextNormalizer class to build a decouple TN layer
- update all impacted python files/classes when required

refactor tts text normalization function in class EPubProcessor in epub.py: 1. create new class TextNormalizer in file text_normalizer.py; 2.move method normalize_text() and all its dependencies to TextNormalizer class; 3.try best to move all text normalization function code to TextNormalizer class to build a decouple TN layer; 4.update all impacted python files/classes when required.

move extract_custom_model() in functions.py into class EbookAudio in ebook_audio.py
- update all impacted python files when required
- don't change existing logic
- try best to add detailed docstring and inline comments


move functions below in functions.py into class EPubProcessor
- filter_chapter
- get_sentences
- get_chapters
- get_cover
- get_ebook_title


add document for this method with detailed inline comments

1. add document for this class and constructor
2. list all session values required in the class

move all nested functions inside launch() to WebUI class method
- only update impacted reference code inside launch() for WebUI
- don't change any other existing logic
- don't try extra refactoring

background:
if input file is epub format, no need to convert
enhancement: 
- check input_file. if it's epub format, directly assign it to epub_path and return 
- don't change any other existing logic
- update docstring too



background:
if input file is epub format, no need to convert

enhance the app to support offline_mode for stanza, hf_hub_download, torch.load(), etc.
- add offline_mode arguments at the end of options in app.py main() so that won't impact existing logic
- update offline_mode to session
- check offline_mode value from session for the libs calls, such as tts.load_checkpoint(), torch.load(), stanza.download(), ehance the existing call to read them from cache
- if there is fialure cannot move forward, print out the details about failed on wich file and its expected location
- only need to check py files in /lib folder
- don't change existing logic


- break down whole task into smaller tasks steps, avoid to reach gemini rate limiting

add indexTTS support to the project that similar to voxcpm for both UI and all processors:
- add IndexTTS to TTS_ENGINES in models.py like voxcpm, the default voice is yunyang.wav
- add indextts support in language_tts, same as voxcpm, it only need to support en and zh-CN
- try best to map xtts tune up parameters such as temperature, top_k, top_p, speed to index TTS ones
- try best to add detailed docstring and inline comments
- add IndexTTS UI support in web_ui.py like voxcpm
- add new tts class TTSIndexTTS to initialize IndexTTS2 like example indextts-test.py with 
  a. model_dir is project's model_dir, 
  b. the cfg_path is project's model_dir
  c. take offline_mode value from command line, default value is True if not specified

add CosyVoice support to the project that similar to XTTSv2 for both UI and all processors:
- add CosyVoice to TTS_ENGINES， default_engine_settings in models.py like XTTSv2
- refer to tools\zero_short_speak_id.py and tools\sft_test.py, CosyVoice has 2 available modles in tts_dir by default
  - CosyVoice2-0.5B is default one(internal), zero shot model,the default voice is yunyang.wav
  - CosyVoice-300M-SFT is fine-tuned one, has 5 support speaker_id (voice): '中文女', '中文男', '粤语女', '英文女', '英文男'
  - create find tuned items for CosyVoice-300M-SFT 5 speaker ids, same for UI in web_ui.py
- add CosyVoice support in class Coqui and comply existing patterns
- add CosyVoice support in language_tts, same as XTTSv2, it only need to support en and zh-CN
- try best to add detailed docstring and inline comments
- add CosyVoice UI support in web_ui.py like XTTS-v2


- extract a method for each tts engine such as XTTSv2, bark, vtis, etc. so that it's easy to add more engine support
-  avoid to use if else, instead call them in a conditional chain, which should be easy to add a method for new tts engine
-  each method will return Ture/False, only call next method in the chain if previous return False
- it's better can use funtional approach


refactor the method
- extract a method for each tts engine 语音合成 such as XTTSv2, bark, vtis, etc. so that it's easy to add more engine support
-  avoid to use if else, instead call them in a conditional chain, which should be easy to add a method for new tts engine
- it's better can use funtional approach



Added CosyVoice as a first-class engine (lib/models.py) with XTTS-like defaults, dual model entries (CosyVoice2-0.5B zero-shot with default voices/zho/adult/male/yunyang.wav, CosyVoice-300M-SFT with five speaker IDs), and language gating for eng/zh-CN (lib/lang.py).
Extended Coqui TTS wrapper to load CosyVoice models, cache zero-shot speakers, and synthesize both zero-shot and SFT flows while preserving samplerate handling and trim behaviour (lib/classes/tts_engines/coqui.py); headless sessions now fall back to valid CosyVoice fine-tuned choices and default speaker IDs (lib/headless_processor.py).
Updated the UI to surface CosyVoice with the XTTS parameter tab, auto-fix fine-tuned selection, and present the five SFT speaker IDs in the voice dropdown while hiding preview/delete controls for those fixed voices (lib/web_ui.py).



create pytest test cases to test class TextNormalizer
- put all test cases into test file tests\test_text_normalizer.py
- create test cases for all class methods
- create test cases and related fixtures to cover all use cases the class and methods support
- create test cases s and related fixtures to cover both english and chinesese use casese
- don't use any mock, provide the reqired and real test data/fixture for testing
- should reach 100% code coverage for TextNormalizer




Updated TextNormalizer to hold is_num2words_compat as an instance var set only in __init__, 
removed the flag from all method signatures, 
and wired all call sites to rely on the instance state. 
This keeps compatibility logic internal and removes the external dependency on the flag across the codebase and tests. 
Changes are in text_normalizer.py, epub.py, functions.py, test_text_normalizer.py, and test_epub.py.


Updated TextNormalizer to hold lang_iso1 as an instance var set only in __init__, 
removed the flag from all method signatures, 
and wired all call sites to rely on the instance state. 
This keeps compatibility logic internal and removes the external dependency on the flag across the codebase and tests. 
Changes are in text_normalizer.py, epub.py, functions.py, test_text_normalizer.py, and test_epub.py.


Implemented the English drop-cap span merge inside the HTML traversal and wired the language code into the iterator 
so single-letter <span>s at sentence/paragraph starts are combined with the following word without inserting a space. 
This lives in the tag iterator that yields text tuples and is applied in epub.py.

Details:

Added pending-span buffering and whitespace-aware merge logic in epub.py.
Passed lan_code through recursive iterator calls and from the chapter extraction entry point in epub.py.
I didn’t run tests (not requested).

Next steps (optional):

Run your normal EPUB processing flow on an English book with drop caps to validate output.
Add a small regression test around _tagged_tuple_paragraphs_iterator for span drop caps.